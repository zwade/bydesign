<strong>By Design</strong> is an open source blogging platform that allows for multiple bloggers to use a single platform to share thoughts and ideas with the world. It has a wide range of features for editors and caching support for added speed.   

<div class="profiles">
<table class="profile">
{{#each (profiles)}}
{{#if this.bio}}
	<tr>
		<th><a href="/author/{{this.handle}}"><img class="author-image large" src="/images/{{#if this.image}}{{this.image}}{{else}}default.png{{/if}}"/></a></th>
		<td>
			<div class="bio">{{this.bio}}</div>
			<div class="contact">
				<address>
					<a href="mailto:{{this.handle}}@dttw.tech">{{this.handle}}@dttw.tech</a>
				</address>
				<address>
					{{#if this.website}}
					<a href="{{this.website}}">{{this.website}}</a>
					{{/if}}
				</address>
				<address>
					{{#if this.github}}
					<a href="mailto:{{this.handle}}@dttw.tech">{{this.handle}}@dttw.tech</a>
					{{/if}}
				</address>
			</div>
		</td>
	</tr>
{{/if}}
{{/each}}
</table>
